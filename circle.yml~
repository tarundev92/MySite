deployment:
  production:
    branch: production
    commands:
      - ./deploy_prod.sh
  staging:
    branch: master
    commands:
      - ./deploy_master.sh
  Features:
    branch: poll
    commands:
      - ./deploy_feature.sh
